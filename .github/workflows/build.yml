name: Build

on: [push]

jobs:
  myjob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: main
      - uses: actions/checkout@v2
        with:
          path: vdpm
      - run: ls $workspace
        env:
          workspace: ${{ github.workspace }}
